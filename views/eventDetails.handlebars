<h1>{{info.eventName}}</h1>
<h3>Posted by {{info.postedBy}} from {{info.college}}</h3>
<p id="desc">{{info.description}}</p>
<p>from {{info.startTime}} to {{info.endTime}}</p>
<p>Hosted at {{info.location}}</p>
<p>Capacity: {{info.usersRegistered.length}}/{{info.capacity}}</p>
{{#if owner}} <p>Currently registered: {{info.usersRegistered}}</p> {{/if}}
{{#each info.tags}}<p class="tag">#{{this}} </p>{{/each}}

{{#if owner}}
<h2 class="header2">Edit Event:</h2>

<div class="error" id="error0" hidden>Im an error</div>

{{#if info.cancelled}}
<form method="POST" action="/events/uncancel/{{info._id}}" id="cancel-btn">
    <input type="submit" value="Uncancel For This Event For All Users">
</form>
{{else}}
<form method="POST" action="/events/cancel/{{info._id}}" id="cancel-btn">
    <input type="submit" value="Cancel For This Event For All Users">
</form>
{{/if}}

<form method="POST" action="/created/{{info._id}}" id="editevent-form" encType="multipart/form-data">
    <label for="eventName">Event name:</label>
    <input type="text" id="editeventName" name="eventName"><br>
    <label for="location">Location:</label>
    <input type="text" id="editlocation" name="location"><br>
    <label for="startTime">Start time:</label>
    <input type="datetime-local" id="editstartTime" name="startTime"><br>
    <label for="endTime">End time:</label>
    <input type="datetime-local" id="editendTime" name="endTime"><br>
    <label for="description">Description:</label>
    <input type="text" id="editdescription" name="description"><br>
    <label for="capacity">Capacity:</label>
    <input type="number" min="1" id="editcapacity" name="capacity"><br>
    <label for="tags">Tags:</label>
    <input type="text" id="edittags" name="tags"><br>
    <label for="image">Upload image here:</label>
    <input type="file" id="editimage" name="image"><br>
    <button type="submit" class="submitbutton">Submit</button>
</form>
{{/if}}

{{#if registered}}
<form method="POST" action="/events/unregister/{{info._id}}" id="register-btn">
    <input type="submit" value="Unregister For This Event">
</form>
{{else}}
<form method="POST" action="/events/register/{{info._id}}" id="register-btn">
    <input type="submit" value="Register For This Event">
</form>
{{/if}}

{{#if favorited}}
<form method="POST" action="/events/unfavorite/{{info._id}}" id="favorite-btn">
    <input type="submit" value="Remove From Favorites">
</form>
{{else}}
<form method="POST" action="/events/favorite/{{info._id}}" id="favorite-btn">
    <input type="submit" value="Add Event To Favorites">
</form>
{{/if}}



<form method="POST" action="/comments/{{info._id}}" id="makecomment-form">
    <label for="comment">Comment:</label>
    <input type="text" id="comment" name="comment">
    <button type="submit">submit</button>
</form>

<div id="allComments">
    {{#each info.comments}} 
        <div class="comment">
            <h2>{{userName}} commented on {{commentDate}}:</h2>
            <p>{{body}}</p>
        </div>
    {{/each}}
</div>

<div id="error" hidden>I'm an error</div>
