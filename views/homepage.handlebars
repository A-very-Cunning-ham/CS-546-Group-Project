{{#if loggedIn}}
    <h1>Events At {{college}}</h1>

    {{#if searchTerm}}
    <h2>Results matching search term "{{searchTerm}}":</h2>
    {{/if}}
    
{{else}}
    <h1>Welcome! Here's a sample of upcoming events from other schools</h1>
    <p id="welcome">For events tailored to your location, please create an account or log in with your existing account.</p>
{{/if}}
{{> partialEvents parameter=event}}